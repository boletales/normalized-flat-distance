# 「平地換算距離」 ― 生理的負荷に基づくサイクリングコースの加法的難易度指標の提案
## 序論
登山コースには加法的な難易度指標がある。登山情報共有サイト YAMAP は「コース定数」という複数の経路間で足せる便利な指標を表示し、「指数が合計12未満ならやさしい、25以上ならきつめ」と目安を示して、登山計画作成の一助としている。

サイクリングコースの難易度も距離や登った高さの合計（獲得標高）で間接的に表されるが、これらを一次元の指標にまとめられるだろうか。たとえば、次の四つのコースを考える。

- 平坦$`100\,\mathrm{km}`$
- $`2\%`$の登り$`10\,\mathrm{km}`$
- $`4\%`$の登り$`5\,\mathrm{km}`$
- $`20\%`$の登り$`1\,\mathrm{km}`$

獲得標高を見れば$`2\%`$ $`10\,\mathrm{km}`$、$`4\%`$ $`5\,\mathrm{km}`$、$`20\%`$ $`1\,\mathrm{km}`$はすべて同じ標高 $`200\,\mathrm{m}`$ 分の上り坂であるが、$`20\%`$ の強烈な急斜面はたとえ短くても $`2, 4\%`$ の緩斜面より明らかに完走困難である。おおまかな目安として、$`10\%`$ を超える坂は「激坂」と呼ばれ、ママチャリでの登坂は難しい。

また、$`2\%`$ $`10\,\mathrm{km}`$ と平坦 $`100\,\mathrm{km}`$ を比較すれば後者のほうが大変であるが、距離と高さを統一的に扱う手段なしにこれらの比較はできない。

ところで、スポーツ科学では、自転車を走らせるために投入する仕事率「パワー」を測定して瞬間的な運動負荷を定量的に扱う方法が普及している。走行に必要なパワーは速度と勾配から計算でき、たとえば「平坦路を$`31\,\mathrm{km/h}`$で走るのと、$`4\%`$の坂を$`19\,\mathrm{km/h}`$で走るのはいずれも$`200\,\mathrm{W}`$のパワーが必要である」というように、平坦路と斜路で同一の基準を用いた議論ができる。与えられたコースに対し、各点での予想走行速度を定めれば、パワーを計算し、コース全体の運動負荷を定量的に評価することができるだろう。

評価にあたって、速度-パワーの関係が斜度依存的であることに留意せねばならない。次節で触れる通り、平坦路においては空気抵抗に起因する三次の項が、急坂路では位置エネルギー変化に起因する一次の項が支配的である。

本稿では、サイクリングコースを走破する「つらさ」をスポーツ科学的側面から考察して定義し、一定の「つらさ」でタイムを短くする良いペーシングを最適化問題として定式化する。この問題の解からは、サイクルスポーツで広く知られている事実が再発見される：

- 初心者ほど登りをつらく感じる。
- 平坦路ではほどほどに手を抜き、登りでは頑張ったほうがよい。
- まともに登れる坂であれば、獲得標高はつらさのよい指標である。

さらに、最適なペースはコース・力量によらず「平地をどの速度で走るか」のみによって決まり、急坂ではコースや力量によらず平地の1.35倍程度のパワーで走るべきであるという普遍的結論を得た。

そののち、得られたペーシングに現実的な修正を加えて、サイクリングコースの大変さを表す新しい指標「平地換算距離」を提案する。

## 物理的・生理学的前提

### パワーのモデル

サイクリングの客観的な運動負荷は、サイクリストがクランクに対して発揮する仕事率（「パワー」と呼ばれる）によって測定される。無風の条件下で角度 $`\theta`$ の道路を速度 $`v`$ で定速走行する場合のパワー $`P_{\mathrm{precise}}(\theta, v)`$ は、次の式で表される：

```math
P_{\mathrm{precise}}(\theta, v) = \frac{1}{\eta}(\frac{1}{2}\rho C_dA\,v^3 + mg(\sin\theta + C_{rr}\cos\theta)\,v + mv\frac{dv}{dt})
```

ここで、ρは空気の密度、Cdは空気抵抗係数、Aは前面投影面積、mは総質量、gは重力加速度、Crrは転がり抵抗係数、ηは駆動効率である。これらの係数は、サイクリングの一般的な条件下ではおおむね定数であるとみなすことができる。
これらの大まかな値を示すと、気温20℃、海面上の空気の密度はおよそ$`1.2\,\mathrm{kg/m^3}`$である。ロードバイクの場合、$`C_dA`$はおよそ$`0.3`$〜$`0.5\,\mathrm{m^2}`$、$`C_{rr}`$はおよそ$`0.005`$、$`\eta`$は$`95`$〜$`98\%`$である。また、車重は$`6`$〜$`10\,\mathrm{kg}`$、サイクリストの体重は$`50`$〜$`100\,\mathrm{kg}`$程度であり、$`100`$〜$`300\,\mathrm{W}`$程度のパワーを継続して発揮できる。

指標を単純にするため、運動エネルギーの変化に関わる項 $`\frac{mv}{\eta}\frac{dv}{dt}`$ は微小であるとして無視する。定速走行に必要なパワーと比較して、サイクリスト・自転車の持つ運動エネルギーは大きくない。全質量$`70\,\mathrm{kg}`$、速度$`10\,\mathrm{m/s}`$のときの運動エネルギーは$`3500\,\mathrm{J}`$であるが、これは位置エネルギー$`5\,\mathrm{m}`$分であり、この速度域での空気抵抗の仕事率は$`250\,\mathrm{W}`$に達する。

また、抵抗に関わる各種係数は、一回のサイクリングで大きく変わらないため定数であるとみなす。これ以降、三次の係数を $`a \in \mathbb{R}`$ 、一次の係数を $`b : [-\pi/2, \pi/2] \to \mathbb{R}`$ つまり、

```math
a := \frac{\rho C_dA}{2\eta}, \qquad b(\theta) := \frac{1}{\eta}mg(\sin\theta + C_{rr}\cos\theta)
```

にまとめて、$`P_{\mathrm{precise}}`$ の加減速を無視した近似値

```math
P_{\mathrm{approx}}(\theta, v) := av^3 + b(\theta)v
```

を考える。$`P_{\mathrm{approx}}`$ は速度の変化を釣り合わせるために必要なパワーであるから、これ以降「均衡パワー」と呼ぶ。

### 主観的な運動負荷のモデル

Andrew Coggan らは、サイクリストの発揮したパワーと筋肉中の乳酸濃度(後者がおよそ前者の4乗に比例する)の関係から、Normalized Power® (NP)を提案した。これは、ある回の運動で発揮したパワーを30秒移動平均で平滑化したのち、L4ノルムを取ることで計算される。NPはStrava、Wahooを始めとしたアクティビティ記録サービスで採用され、広く親しまれた指標であり、達成できるNPの上限はサイクリストの力量によっておおまかに決まることが知られている。

NPの計算方式において、パワーの四乗は加法的な指標とみなされている。ここでは、コースをある定められた速度分布のもと走った際の均衡パワーの四乗の時間積分（今後「合計つらさ」と呼ぶ）をサイクリストの主観的な運動負荷の指標とする。

## 数理的考察

### コースとつらさの定義

はじめに、コースや走行について定義しておく。

**Def. 1** 正実数 $`L`$ 、関数 $`G: [0, L] \to [-\pi/2, \pi/2]`$ の組 $`(L, G)`$ を「コース」と呼ぶ。$`L`$ を「コース長」と呼び、$`x \in [0, L]`$ に対して $`G(x)`$ を「$`x`$ における勾配」と呼ぶ。

**Def. 2** 正実数 $`a`$、関数 $`b: [-\pi/2, \pi/2] \to \mathbb{R}`$ の組 $`(a, b)`$ を「抵抗モデル」と呼ぶ。$`a`$ を「空気抵抗係数」と呼び、$`\theta \in [-\pi/2, \pi/2]`$ に対して $`b(\theta)`$ を「重力抵抗係数」と呼ぶ。正実数 $`v`$ と $`\theta \in [-\pi/2, \pi/2]`$ に対して $`av^3 + b(\theta)v`$ を「勾配 $`\theta`$、速度 $`v`$ での均衡パワー」と呼ぶ。

**Def. 3** コース $`C (= (L, G))`$、抵抗モデル $`M`$、正実数 $`T`$、微分可能で単調増加な全単射 $`D: [0, T] \to [0, L]`$ で、$`D(0)=0`$ かつ $`D(T)=L`$ であるものの組 $`(C, M, T, D)`$ を「走行」（もしくは「走行の時間表示」）と呼ぶ。$`T`$ を「走行時間」と呼び、$`t \in [0, T]`$ に対して $`D(t)`$ を「$`t`$ における位置」と呼ぶ。

**Def. 4** 走行 $`R (= ((L, G), M, T, D))`$ と $`t \in [0, T]`$ に対して、$`R`$ の $`t`$ における

- 「瞬間速度」を、$`v := dD/dt(t)`$ と定める。
- 「瞬間均衡パワー」を、抵抗モデル $`M`$ の勾配 $`G(D(t))`$、速度 $`v(t)`$ での均衡パワーで定める。
- 「瞬間つらさ」を、$`S := P^4`$ と定める。

**Def. 5** 走行 $`R (= (C, M, T, D))`$ に対して「合計つらさ」を、$`R`$ の $`t`$ における瞬間つらさを $`S(t)`$ として、$`\int_0^T S(t)\,dt`$ と定める。

**Def. 6** 走行 $`R`$ に対して、$`R`$ の合計つらさ/$`R`$ の走行時間 を「平均つらさ」と呼ぶ。（これはつまり、NPのようなものである）

**Def. 7** 走行 $`R (= ((L, G), M, T, D))`$ と $`x \in [0, L]`$ に対して、$`R`$ の $`x`$ における

- 「通過時刻」を、$`t := D^{-1}(x)`$ と定める。
- 「地点速度」を、$`v`$ を $`t`$ における瞬間速度であると定める。
- 「地点均衡パワー」を、$`t`$ における瞬間均衡パワーで定める。これは抵抗モデル $`M`$ の三次・一次の係数をそれぞれ $`a, b(G(x))`$ として、$`av^3 + b(G(x))v`$ である。
- 「地点つらさ」を、$`t`$ における瞬間つらさを $`v`$ で除したものと定める。

**Prop. 1** 走行 $`R`$ に対して、$`R`$ の合計つらさは、$`R`$ の $`x`$ における地点つらさを $`S(x)`$ として、$`\int_0^L S(x)\,dx`$ と等しい。(証明略)

**Def. 8** コース $`C (= (L, G))`$、抵抗モデル $`M`$ と、$`v: [0, L] \to \mathbb{R}_+`$ であって $`T=\int_0^L 1/v(x)\,dx`$ が有限なものの組 $`(C, M, v)`$ を「走行の速度表示」と呼ぶ。

**Prop. 2** 走行の時間表示と速度表示は以下の全単射で対応する。すなわち、

- 走行の時間表示 $`R (= (C, M, T, D))`$ に対して、$`R`$ の $`x`$ における地点速度を $`v(x)`$ として、速度表示 $`(C, M, v)`$ を対応付ける。
- 走行の速度表示 $`(C, M, v)`$ に対して、$`T=\int_0^L 1/v(x)\,dx`$ と $`t(x)=\int_0^x 1/v(s)\,ds`$ として、時間表示 $`(C, M, T, t^{-1})`$ を対応付ける。

を考えると、これらは互いの逆写像である。（証明略）

そのため、今後は走行の時間表示と速度表示を同一視して、単に「走行 $`R (= (C, M, v))`$」と書くことにする。

**Def. 9** コース $`C_1 (= (L_1, G_1))`$、$`C_2 (= (L_2, G_2))`$ について、「$`C_1`$ と $`C_2`$ の結合」とは、コース $`C_3 (= (L_1 + L_2, G_3))`$ である。ここで、$`G_3(x) := x < L_1`$ なら $`G_1(x)`$、さもなくば $`G_2(x - L_1)`$ とする。

**Def. 10** 抵抗モデルが同じ走行(速度表示) $`R_1 (= (C_1, M, v_1))`$、$`R_2 (= (C_2, M, v_2))`$ について、「$`R_1`$ と $`R_2`$ の結合」とは、走行 $`R_3 (= (C_3, M, v_3))`$ である。ここで、$`C_3`$ は $`C_1`$ と $`C_2`$ の結合であり、$`v_3(x) := x < L_1`$ なら $`v_1(x)`$、さもなくば $`v_2(x - L_1)`$ とする。定義より明らかだが、結合の走行時間は $`R_1`$ と $`R_2`$ の走行時間の和である。

**Prop. 3** 合計つらさは加法的である。すなわち、抵抗モデルが同じ走行 $`R_1, R_2`$ に対して、$`R_1`$ と $`R_2`$ の結合の合計つらさは、これらの合計つらさの和である。（合計つらさの定義より直ちに明らかである）

### 走行の最適化

#### 概略

サイクリストが達成できるNPは力量によって上限が定まる。サイクリングにおいて、定められたNP以下で最も早くコースを走り切る走行を考えるのは自然な問題設定である。（「同じタイムでNPが最小の走行」を考えても、同種の問題に帰着する）

本節では、前節で定義した「平均つらさ」を一定に保ちつつ、走行時間を最小化する最適な走行がどのようものであるか考えよう。定性的には、次のような結論が得られる。

- 各地点で発揮すべきパワーは、勾配のみによって定まる。
- 平地ではパワーを少なめに、登りではパワーを多めに発揮するべきである。（平地で飛ばしても空気抵抗が増えるばかりで割に合わないため、このような配分になる。この結果は我々の実感と符合する。）
- 急坂路において、つらさはおよそ勾配に比例する。
- 急坂路でのパワーは抵抗モデルによらず平地の1.3〜1.38倍程度になる。
- 非自明な結論として、最適なペーシングは抵抗モデルと「ある勾配における速度/パワー」を定めると、コースにかかわらず一意に定まる。

特に、パワーに関する諸結論がサイクリングの一般的アドバイスと整合的であることは次節が与える結論の妥当性を支持するものである。

#### 定式化

特定の抵抗モデル $`M (= (a, b))`$ について、各コースで最適な走行はどのようなものか調べてみよう。

仮の問題設定として、サイクリストの力量を表す正実数 $`S_0`$ とコース $`C (= (L, G))`$ が与えられたとき、$`v: [0, L] \to \mathbb{R}_+`$ であって、走行 $`(C, M, v)`$ の平均つらさが $`S_0`$ であり、走行時間が最小となるものを考える。

この走行の合計つらさは、各点 $`x`$ における地点速度を $`v(x)`$ として $`\int_0^L (a v(x)^3 + b(G(x)) v(x))^4 dx`$ である。また、走行時間は $`\int_0^L 1/v(x)\,dx`$ である。これらを用いて、平均つらさが $`S_0`$ であることとは、次の等式が満たされることである。

```math
\left(\int_0^L \frac{(a v(x)^3 + b(G(x))v(x))^4}{v(x)}\,dx\right) / \left(\int_0^L \frac{1}{v(x)}\,dx\right) = S_0
```

この条件は、合計つらさに関する制約として、次のように書き換えることもできる。

```math
\left(\int_0^L \frac{(a v(x)^3 + b(G(x))v(x))^4}{v(x)}\,dx\right) - \left(S_0\int_0^L \frac{1}{v(x)}\,dx\right) = 0
```

積分区間が同じであるからまとめてしまってよい：

```math
\int_0^L \left(\frac{(a v(x)^3 + b(G(x))v(x))^4}{v(x)} - \frac{S_0}{v(x)}\right)dx = 0
```

ラグランジュの未定乗数法を用いる。次のような関数を考える。

```math
F(v, \lambda) := \int_0^L \left(\frac{1}{v(x)} + \lambda\left(\frac{(a v(x)^3 + b(G(x))v(x))^4}{v(x)} - \frac{S_0}{v(x)}\right)\right)dx
```

この最適化問題の解は、上式の $`v, \lambda`$ に関する極値である。ひとまず $`v`$ での偏微分について考えてみよう。

上式には $`v`$ の微分に関する項が出現しないため、各地点について独立に最適化することができる。ここで、被積分関数について考える（以下、この節内では $`b(G(x))`$ は単に $`b`$、$`v(x)`$ は単に $`v`$ と書く）。

```math
f(v, \lambda) := \frac{1}{v} + \lambda\left(\frac{(av^3 + bv)^4}{v} - \frac{S_0}{v}\right)
= \lambda(av^2 + b)^4 v^3 + \frac{1 - \lambda S_0}{v}
```

この関数の $`v`$ に関する偏微分は、

```math
\frac{\partial f}{\partial v} = \lambda(11av^2 + 3b)(av^2 + b)^3 v^2 - \frac{1 - \lambda S_0}{v^2}
```

となって、これが0であるならば、$`v`$ に関する方程式

```math
(11av^2 + 3b)(av^2 + b)^3 v^4 = \frac{1}{\lambda} - S_0
```

が満たされる。

#### 戦略的な意味

上式の持つ意味について考えたい。三次抵抗係数が $`a`$、一次抵抗係数が $`b`$、いまの走行速度が $`v`$ だったとして、微小区間の走行時間を少し変えるとつらさはどう変わるだろうか？これは、$`t := 1/v`$ とおいて、$`S := (av^3 + bv)^4 / v`$ の $`t`$ に関する偏微分 $`\delta S/\delta t`$ を考えることにほかならない。計算すると、$`\delta S/\delta t = -(11av^2 + 3b)(av^2 + b)^3 v^4`$ となり、これは上式左辺の-1倍である。

つまり、$`(11av^2 + 3b)(av^2 + b)^3 v^4 = 1/\lambda - S_0`$ というのは、$`\delta S/\delta t = S_0 - 1/\lambda`$ という意味であり、端的に言えば「つらさを増やしてタイム短縮を購入するとき、どの地点も同じ値段 $`S_0 - 1/\lambda`$ になるようにする」という戦略である。

#### 最適速度

さて、ここまでの議論ではコース $`C`$ と力量 $`S_0`$ の二つがなければ $`\lambda`$ が定まらなかったが、上述の結論を $`\delta S/\delta t = \mathrm{const.}`$ と読み替えると、コースにかかわらず普遍的な結論を得ることができる。

平均つらさ $`S_0`$ を定めるかわりに、あるパワー $`P_0`$ を与え、最適ペーシングのうち平坦路で $`P_0`$ を発揮するものを考えてみよう。このような問題設定の変更は合理的である。たのしくロングライドするのが目的であれば、コースによらず、勾配ごとにそれぞれ「いつもどおり」のペースで走るのが自然であろう。

**Def. 11** 抵抗モデル $`M (= (a, b))`$ 、「平地パワー」正実数 $`P_0`$ と、$`\theta\in[-\pi/2, \pi/2]`$ に対し、「最適速度 $`v_{\mathrm{op}}(M, P_0, \theta)`$」を次のように定める：

- $`v_0`$ を、$`av_0^3 + b(0)v_0 = P_0`$ を満たす正の値として定める。
- $`c := (11av_0^2 + 3b(0))(av_0^2 + b(0))^3 v_0^4`$ とおく。（これを「最適コスト」と呼ぶ）
- $`v_{\mathrm{op}}(M, P_0, \theta)`$ を、$`(11av^2 + 3b(\theta))(av^2 + b(\theta))^3 v^4 = c`$ を満たす最大の $`v`$ とする。（この方程式はかならず均衡パワーが正になるような正速度の解を持つ。$`b(G(x))<0`$ のとき正の解を複数持つが、最大以外の解は均衡パワーが負の値になる速度であり、負の仕事をしても脚が楽になることはない。）

適当なコース $`C`$ について、走行 $`R (= (C, M, \text{各点 } x \text{ に対し } v_{\mathrm{op}}(M, P_0, G(x))))`$ を考えると、先の議論から、これは $`R`$ と同じ平均つらさを持つ $`C`$ の走行の中で最速のものである。

最適速度と同様に、最適パワー、最適つらさについても定義する。これらの定性的な振る舞いは次節で議論する。

**Def. 12** 抵抗モデル $`M (= (a, b))`$、「平地パワー」正実数 $`P_0`$ と、$`\theta\in[-\pi/2, \pi/2]`$ に対し、「最適パワー $`P_{\mathrm{op}}(M, P_0, \theta)`$」を、$`P_{\mathrm{op}}(M, P_0, \theta) := av_{\mathrm{op}}(M, P_0, \theta)^3 + b(\theta)v_{\mathrm{op}}(M, P_0, \theta)`$ と定める。また、「最適つらさ $`S_{\mathrm{op}}(M, P_0, \theta)`$」を、$`S_{\mathrm{op}}(M, P_0, \theta) := P_{\mathrm{op}}(M, P_0, \theta)^4 / v_{\mathrm{op}}(M, P_0, \theta)`$ と定める。なお、上述のとおり、最適パワーはかならず正である。

かくして、我々は「平地をある特定のパワーで走る」という仮定のもと、勾配ごとに最適な速度・パワーが定まること、またそれがコースによらないことを示した。

### 最適なペーシングの性質

#### 速度・パワー・つらさの概形

最適なペーシングでは、勾配に対して速度・パワー・区間つらさがどう変化するか調べてみよう。最適コストを $`c`$ とする。

抵抗のうち一次の係数の内実は、$`b(\theta) = mg(\sin\theta + C_{rr}\cos\theta)/\eta`$ であったから、おおむね $`b(\theta) \propto \theta`$ である。最適コストと速度の関係は $`(11av^2 + 3b)(av^2 + b)^3 v^4 = c`$ であったから、最適速度は勾配に対して単調減少である。

ある区間の最適速度が $`v`$、パワーが $`P`$ だったとして、コストの条件を変形すると $`c = P^3(3P + 8av^3)`$ となるから、$`v`$ と $`P`$ はトレードオフの関係にあることがわかる。したがって、パワーは勾配に対して単調増加である。速度が単調減少でパワーが単調増加なのだから、区間つらさが勾配に対して単調増加であることもわかる。

#### 急坂路での概形

空気抵抗が無視できる急坂路では、増減以上に強い結果が得られる。$`\theta`$ が大きいと $`v_{\mathrm{op}}(M, P_0, \theta)`$ が小さくなるため、速度の二乗の項は無視して近似できる。

この領域では、コストと速度の関係は $`c \approx 3P^4 \approx 3(vb(\theta))^4`$ であるから、最適速度は $`v_{\mathrm{op}}(M, P_0, \theta) \approx (c/3)^{1/4} / b(\theta) \propto 1/\theta`$ である。

さらに、平地パワー $`P_0`$ と $`v_{\mathrm{op}}(M, P_0, \theta)`$ の比を考えてみよう。
$`b(0)`$ を 0 とみなすと、$`P_0 \approx av_0^3`$ であるから、最適コストは $`c \approx 11P_0^4`$ である。

一方、$`P_{\mathrm{op}}(M, P_0, \theta) \approx b(\theta)v_{\mathrm{op}}(M, P_0, \theta)`$ であるから、これらを合わせて $`3P_{\mathrm{op}}(M, P_0, \theta)^4 \approx 11P_0^4`$ となる。つまり、急坂路での最適パワーは平地パワーの $`(11/3)^{1/4} \approx 1.38`$ 倍程度になることがわかる。（実際には $`b(0)`$ は0でないので、倍率はこれより少し低くなる。

上述の議論より、急坂路において速度は斜度に反比例し、パワーはおおよそ一定であるから、区間つらさは勾配に対して一次関数に漸近することになる。

#### 急坂路の相対的つらさの $`P_0`$ 依存性

平地と急坂路の区間つらさの比を考える。平地の区間つらさはおよそ $`P_0^4 / v_0 \approx P_0^4 / (P_0/a)^{1/3} = a^{1/3} P_0^{11/3}`$ である。一方、急坂路の区間つらさはおよそ $`(11/3\,P_0^4) / v_{\mathrm{op}}(M, P_0, \theta) \approx (11/3\,P_0^4) / ((11/3)^{1/4} P_0 / \theta) = (11/3)^{3/4}\theta P_0^3`$ である。
したがって、急坂路の区間つらさは平地の区間つらさの $`(11/3)^{1/4} a^{-1/3} P_0^{-2/3}\theta`$ 倍程度になることがわかる。この結果の特筆すべき点は三つである：

- 急坂路において、距離あたりのつらさはおおむね勾配に比例する。
- 漸近線の比例係数は $`a^{-1/3}`$ に比例する。空気抵抗係数が大きいと平地がつらくなるため、空気抵抗の影響が少ない急坂路の相対的なつらさは小さくなる。
- 漸近線の比例係数は $`P_0^{-2/3}`$ に比例する。パワーが大きいとペースがあがるが、つらさの増大は急坂路より平地のほうが大きい。

ここから「$`P_0`$ の小さな初心者ほど相対的に坂をつらく感じる」という主張が得られるが、これはサイクリストの実感とよく符合する。

#### 最適速度の数値解について

$`v_{\mathrm{op}}(M, P_0, \theta)`$ に関する方程式の解を数値的に求める際の考察として、コストと速度の関係を抜き出した関数 $`f(v) := (11av^2 + 3b)(av^2 + b)^3 v^4`$ の振る舞いを調べる。

- $`g(V) := (11aV + 3b)(aV + b)^3 V^2`$ とおくと、$`f(v) = g(v^2)`$ である。我々は正の速度にしか興味がないので、求解にあたっては $`g(V) = c`$ を解き、その平方根を取ればよい。
- $`g'(V) = 6V (aV + b)^2 (11a^2V^2 + 8abV + b^2)`$ である。
- $`b`$ の正負で解の挙動が変わる。
  - $`b`$ が正ならば、$`V>0`$ で $`g`$ は単調増加である。$`v_0`$ などから出発してニュートン法で解を求めるなどすればよい。
  - $`b`$ が負ならば、$`V=0`$, $`-((4\pm\sqrt{5})/11)\,b/a`$（それぞれ $`-0.16b/a`$, $`-0.56b/a`$ 程度）, $`-b/a`$ で $`g'(V)=0`$ となる。我々が興味のある $`V`$ は $`-b/a`$ より大きいので、$`-2b/a`$ などから出発してニュートン法で解を求めるなどすればよい。

## 指標の提案

### 平地換算距離

ここまでで得られた最適ペーシングを用いて、サイクリングコースの大変さを表す指標「平地換算距離」（NFD: Normalized Flat Distance）を提案する。これは、コース全体のつらさを、平地を走るときのつらさで正規化したものである。

**Def. 13** 抵抗モデル $`M`$、平地パワー $`P_0`$、正実数 $`v_{\min}`$ と、斜度 $`\theta\in[-\pi/2, \pi/2]`$ に対して、

- 「想定速度 $`v_{\mathrm{est}}(M, P_0, \theta)`$」を、$`\max(v_{\mathrm{op}}(M, P_0, \theta), v_{\min})`$ と定める。
- 「想定パワー $`P_{\mathrm{est}}(M, P_0, \theta)`$」を、$`a v_{\mathrm{est}}(M, P_0, \theta)^3 + b(\theta)v_{\mathrm{est}}(M, P_0, \theta)`$ と定める。
- 「想定区間つらさ $`S_{\mathrm{est}}(M, P_0, \theta)`$」を、$`P_{\mathrm{est}}(M, P_0, \theta)^4 / v_{\mathrm{est}}(M, P_0, \theta)`$ と定める。
- 「勾配係数 $`k(M, P_0, \theta)`$」を、$`S_{\mathrm{est}}(M, P_0, \theta) / S_{\mathrm{est}}(M, P_0, 0)`$ と定める。

ここで、勾配係数というのは「斜度θののつらさは平地の何倍か」である。

**Def. 14** 抵抗モデル $`M`$、平地パワー $`P_0`$、正実数 $`v_{\min}`$ とコース $`C (= (L, G))`$ に対して、「平地換算距離 $`D_{\mathrm{eq}}(M, P_0, v_{\min}, C)`$」を、$`\int_0^L k(M, P_0, G(x))\,dx`$ と定める。

速度の下限を設けたのは、筋肉の性質上効率的なペダリングができる回転数に下限があり、これ以下の領域では登坂が困難となるためである。典型的な値として、回転数の下限は40rpm程度であり、ロードバイクの最低ギア比はクランク1周あたり2.1m程度であるものが多いが、これを分速40回転で回せば5km/hである。

### 計算手順

平地換算距離の計算手順をあらためてまとめる。

まず、抵抗モデル $`M`$（三次の係数 $`a`$、勾配から一次の係数を得る $`b`$）と平地パワー $`P_0`$ に対して、勾配係数 $`k(M, P_0, \theta)`$ を計算する。

1. 「平地速度」 $`v_0`$ は方程式 $`av_0^3 + b(0)v_0 = P_0`$ の唯一の実数解であるから求める。（$`a, b(0)`$ はいずれも正であるから、これを満たす $`v_0`$ は一意に存在し、正である。）
2. 「最適コスト」 $`c`$ を、$`c := (11av_0^2 + 3b(0))(av_0^2 + b(0))^3 v_0^4`$ とおく。
3. 斜度 $`\theta`$ における最適速度 $`v_{\mathrm{op}}(M, P_0, \theta)`$、すなわち $`v`$ の方程式 $`(11av^2 + 3b(\theta))(av^2 + b(\theta))^3 v^4 = c`$ の最大の実数解を求める。（先に述べた通り、この方程式は常に解を持ち、最大の実数解ではパワーが常に正である。）
4. 斜度 $`\theta`$ における想定速度 $`v_{\mathrm{est}}(M, P_0, \theta)`$ を、$`\max(v_{\mathrm{op}}(M, P_0, \theta), v_{\min})`$ を計算する。
5. 斜度 $`\theta`$ における想定パワー $`P_{\mathrm{est}}(M, P_0, \theta)`$ を、$`av_{\mathrm{est}}(M, P_0, \theta)^3 + b(\theta)v_{\mathrm{est}}(M, P_0, \theta)`$ を計算する。
6. 斜度 $`\theta`$ における想定区間つらさ $`S_{\mathrm{est}}(M, P_0, \theta)`$ を、$`P_{\mathrm{est}}(M, P_0, \theta)^4 / v_{\mathrm{est}}(M, P_0, \theta)`$ を計算する。
7. 斜度 $`\theta`$ における勾配係数 $`k(M, P_0, \theta)`$ を、$`S_{\mathrm{est}}(M, P_0, \theta) / S_{\mathrm{est}}(M, P_0, 0)`$ を計算する。

次に、与えられたコース $`C`$（コース長 $`L`$、勾配関数 $`G`$）について、勾配係数を用いて平地換算距離を計算する。

8. コース $`C`$ の平地換算距離 $`D_{\mathrm{eq}}(M, P_0, v_{\min}, C)`$ を、$`\int_0^L k(M, P_0, G(x))\,dx`$ を計算する。

実用上の留意点として、NFDの計算を行う前に勾配データの平滑化を行うことが推奨される。勾配係数は想定速度が最低速度を下回るような勾配（$`P_0=100\,\mathrm{W}`$ であれば、おおよそ13%以上）では急激に大きな値を取る。既存のルート作成サービスでは標高データを直接用いて計算しているため、エクスポートしたデータには道路の位置ズレや誤差に影響されて実態と乖離した急傾斜が含まれることがある。次節では、サビツキーフィルタを用いて平滑化を行っている。

## 実例

### 勾配係数表

ロードバイクを想定したパラメータとして $`C_dA=0.35\,\mathrm{m^2}`$、$`C_{rr}=0.005`$、$`\eta=0.97`$、$`m=70\,\mathrm{kg}`$、$`v_{\min}=5\,\mathrm{km/h}`$ と設定し、平地パワーを $`100, 150, 200\,\mathrm{W}`$ と設定したときの勾配係数を計算した。これらの値は体重 $`60\,\mathrm{kg}`$、$`P_0`$ を $`60\%`$ FTPと想定すれば、PWRが $`2.7, 4.0, 5.4\,\mathrm{W/kg}`$ に対応し、それぞれ初級者、中級者、上級者として妥当な値である。

$`P_0 = 100\,\mathrm{W}`$ の場合：

| 勾配 [%] | 想定速度 [km/h] | 想定パワー [W] | 勾配係数 [km/km] |
| --- | --- | --- | --- |
| -16 | 81.12 | 35.6 | 0.005024 |
| -14 | 75.99 | 38.0 | 0.006953 |
| -12 | 70.46 | 41.0 | 0.010111 |
| -10 | 64.46 | 44.7 | 0.015688 |
| -8 | 57.91 | 49.7 | 0.026571 |
| -6 | 50.67 | 56.5 | 0.050857 |
| -4 | 42.64 | 66.4 | 0.115415 |
| -2 | 33.92 | 81.1 | 0.321939 |
| 0 | 25.30 | 100.0 | 1.000000 |
| 2 | 18.10 | 116.5 | 2.574801 |
| 4 | 13.02 | 125.3 | 4.793755 |
| 6 | 9.76 | 128.9 | 7.145717 |
| 8 | 7.69 | 130.3 | 9.469568 |
| 10 | 6.31 | 130.9 | 11.755582 |
| 12 | 5.35 | 131.2 | 14.011354 |
| 14 | 5.00 | 141.8 | 20.440840 |
| 16 | 5.00 | 160.8 | 33.808258 |

$`P_0 = 150\,\mathrm{W}`$ の場合：

| 勾配 [%] | 想定速度 [km/h] | 想定パワー [W] | 勾配係数 [km/km] |
| --- | --- | --- | --- |
| -16 | 81.54 | 61.7 | 0.010437 |
| -14 | 76.49 | 65.8 | 0.014318 |
| -12 | 71.09 | 70.7 | 0.020549 |
| -10 | 65.27 | 76.8 | 0.031232 |
| -8 | 58.97 | 84.7 | 0.051114 |
| -6 | 52.15 | 95.1 | 0.092023 |
| -4 | 44.81 | 109.3 | 0.186180 |
| -2 | 37.12 | 127.9 | 0.422601 |
| 0 | 29.64 | 150.0 | 1.000000 |
| 2 | 23.07 | 170.4 | 2.138400 |
| 4 | 17.85 | 184.3 | 3.781918 |
| 6 | 14.03 | 191.8 | 5.647510 |
| 8 | 11.33 | 195.5 | 7.545810 |
| 10 | 9.41 | 197.3 | 9.421338 |
| 12 | 8.02 | 198.2 | 11.267846 |
| 14 | 6.98 | 198.8 | 13.088102 |
| 16 | 6.18 | 199.1 | 14.884603 |

$`P_0 = 200\,\mathrm{W}`$ の場合：

| 勾配 [%] | 想定速度 [km/h] | 想定パワー [W] | 勾配係数 [km/km] |
| --- | --- | --- | --- |
| -16 | 82.01 | 90.8 | 0.017108 |
| -14 | 77.04 | 96.5 | 0.023257 |
| -12 | 71.76 | 103.4 | 0.032938 |
| -10 | 66.11 | 111.9 | 0.049066 |
| -8 | 60.07 | 122.6 | 0.077789 |
| -6 | 53.61 | 136.3 | 0.132948 |
| -4 | 46.80 | 153.8 | 0.246917 |
| -2 | 39.82 | 175.5 | 0.492353 |
| 0 | 33.05 | 200.0 | 1.000000 |
| 2 | 26.94 | 223.2 | 1.902726 |
| 4 | 21.81 | 241.0 | 3.196106 |
| 6 | 17.75 | 252.4 | 4.724987 |
| 8 | 14.66 | 258.9 | 6.333585 |
| 10 | 12.35 | 262.5 | 7.944914 |
| 12 | 10.61 | 264.6 | 9.535781 |
| 14 | 9.28 | 265.8 | 11.102604 |
| 16 | 8.24 | 266.5 | 12.646395 |

### 主要なサイクリングコースのNFD

前節と同様の条件で、主要なサイクリングコースのNFDを計算した。標高データの取得にあたっては OSRM でルートを構成したのち、OSMのウェイの端点の標高を地理院のDEMデータから取得して、25m間隔になるよう線形にリサンプルしてから、次数3・ウインドウサイズ11のサビツキーフィルタを用いて平滑化を行った。NFD・獲得標高は、この平滑化後の標高データを用いて計算している。

| コース | 距離 [km] | 獲得標高 [m] | NFD (100W) [km] | NFD (150W) [km] | NFD (200W) [km] |
| --- | --- | --- | --- | --- | --- |
| 不動峠 | 4.27 | 273 | 33.00 | 25.98 | 21.85 |
| ヤビツ峠 | 11.76 | 663 | 79.37 | 63.21 | 53.28 |
| 都民の森 | 29.22 | 915 | 119.50 | 94.54 | 81.52 |
| 富士ヒルクライム | 24.04 | 1260 | 150.90 | 119.81 | 100.89 |
| 暗峠 | 2.20 | 378 | 151.33 | 42.00 | 29.62 |
| ビワイチ | 193.76 | 392 | 206.09 | 201.00 | 198.28 |

（注：都民の森のスタート地点は武蔵五日市駅に設定している。ビワイチ​のコースは琵琶湖大橋から北湖と南湖を反時計回りに周回するものであり、 https://b-mo.jp/cyclingmap/78.html で紹介されている経路を用いた。）

## 結論

### 今後の課題

今回提案した手法では、出力を均衡パワーに、運動の負荷をその四乗に単純化して考えることで、サイクリングに関する一般的な諸結論と同様の結果や加法的なコース難易度指標を得た。しかしながら、ここまで挙げた手法では、集団走行による空気抵抗の低減、酸素濃度の違いに起因するパワーの変化などは考慮しておらず、乳酸濃度の法則である Coggan の四乗則をそのまま用いることの妥当性も評価していない。より細やかなパワー・運動負荷のモデルに対して同様の手法を適用することで、さらに実戦的な指標を得ることができると考えられる。

### まとめ

本稿では、Coggan の四乗則に基づき、サイクリングを一種の最適化問題として定式化し、サイクルスポーツにおける経験則への理論的な裏付けを与えるとともに、ペーシングのコース非依存性や、最適ペーシングにおける平坦路・急坂路のパワーの比に関する普遍的な結論を得た。

また、定式化の結果を用いて、サイクリングコースの難易度を表す指標「平地換算距離」を提案した。この指標は、コースの勾配プロファイルを用いて計算される加法的な指標であり、コースの難易度を平地距離に換算して表すものである。